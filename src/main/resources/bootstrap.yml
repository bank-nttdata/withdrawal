spring:
  application:
    name: withdrawal
  config:
    import: "optional:configserver:http://localhost:7000"
    #import: "optional:configserver:http://micro-config-server:7000"
  cloud:
    config:
      enabled: true
      label: master
      uri: http://localhost:7000